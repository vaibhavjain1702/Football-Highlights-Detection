# Deep Learning Project - Git Ignore
# This file prevents large or unnecessary files from being pushed to GitHub

# --- LARGE DATA (REJECTED BY GITHUB > 100MB) ---
# SoccerNet_subset is 2.1GB - too large for GitHub
SoccerNet_subset/
soccernet_data_backup.zip

# Raw videos are too large (> 100MB)
Videos/
Main Files/backup_*.zip

# --- CACHES ---
__pycache__/
*.pyc
.DS_Store

# --- ALLOWED FILES ---
# We explicitly allow highlight videos and models under 100MB
!checkpoints/
!Main Files/*.mp4

# --- REDUNDANT NOTEBOOKS ---
Highlight_generation (1).ipynb
Highlight_generation_FINAL.ipynb
